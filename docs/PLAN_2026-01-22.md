# 1-22-2026 Plan — LaunchBox “Thorough” Cleanup (Low-Risk, High-Confidence)

## Goals

1. Reduce breakage risk and future drift by cleaning up `backend/routers/launchbox.py` to be internally consistent, type-checkable, and “boring.”
2. Remove obvious dead/invalid code paths and incomplete endpoints that could fail at runtime.
3. Keep the LaunchBox API contract stable:
   - Canonical: `GET /api/launchbox/games` returns `{ games, total, page, limit }` (paged)
   - Legacy: `GET /api/launchbox/games/list` returns a raw array (compat)
4. Verify end-to-end health using existing smoke scripts and minimal targeted tests.

## Guardrails (Do Not Break Things)

- Only change behavior when:
  - it is clearly wrong / currently broken, or
  - it is covered by an explicit verification step, or
  - it is a strictly additive, backward-compatible improvement.
- If a change has meaningful uncertainty ("might fix A but break B"), skip it and log it as a follow-up item.
- Keep “contract surfaces” stable (request/response shapes for existing endpoints) unless the endpoint is clearly unfinished/broken.

## Pre-Flight (10–20 min)

1. Capture baseline:
   - Run `npm run smoke:stack:no-start` and confirm GREEN.
   - Run `npm run smoke:gateway:no-start` and confirm GREEN.
2. Identify current lint/type/syntax errors before editing:
   - Run `python -m compileall backend` (ensures syntax correctness).
   - In VS Code, run Pylance “Problems” on `backend/routers/launchbox.py` and record top offenders.

## Phase A — Fix the obvious contract/model drift in `/resolve` (30–60 min)

### A1. ResolveRequest field mismatch (high confidence)

Current issue: `ResolveRequest` defines `game_name` with alias `title`, but the endpoint constructs it using `game_name=...` and then reads `data.game_name`.

Plan:
- Make the request model and endpoint consistent.
- Choose a single canonical field name and support backward-compatible aliases.

Preferred approach (most compatible):
- Keep canonical internal name `game_name`.
- Accept aliases: `title`, `game_name`, and `game` if needed.
- Ensure GET query `title=` continues to work.
- Ensure POST body with `{ "title": "..." }` continues to work.

Verification:
- Exercise `GET /api/launchbox/resolve?title=...`.
- Exercise `POST /api/launchbox/resolve` with both `{ "title": ... }` and `{ "game_name": ... }`.

## Phase B — Clean up clearly broken / incomplete code blocks (60–120 min)

### B1. Fix incomplete endpoints / invalid code blocks (very high confidence)

From current file snapshot, at least these look incomplete or obviously broken:

- `preview_shader_change`: the block reading `old_config` appears empty/incomplete.
- `revert_shader_change`: body appears empty.

Plan:
- Either implement these endpoints fully (preferred if straightforward and used), OR disable them safely:
  - Return `501 Not Implemented` with a clear message.
  - Ensure no syntax/indentation errors.

Guardrail:
- Do not guess intended behavior. Implement only what can be inferred safely.

Verification:
- `python -m compileall backend` passes.
- Hit shader endpoints quickly (or ensure they are removed/disabled cleanly).

### B2. Fix the `autofix_retroarch` “core path” block (high confidence)

The section that tries to build `cp` currently has multiple nonsensical assignments (leftover experimentation). It’s risk for future edits and readability.

Plan:
- Replace with a single, correct path construction:
  - `cp = chosen.parent / 'cores' / '<dll>'`
- Keep behavior: detect stella core if present.

Verification:
- Compileall.
- (Optional) Add a small unit test for the path logic if it’s factored into a helper.

## Phase C — Router hygiene + dependency injection correctness (45–90 min)

### C1. Make `LaunchBoxServices` test-friendly (high confidence)

Plan:
- Allow optional injection via `__init__(...)` parameters (default to current globals).
- Keep `get_launchbox_services()` returning default `LaunchBoxServices()`.

Verification:
- No behavior change in production.
- Add a small unit test that overrides dependency and calls one endpoint.

### C2. Remove unused imports / fix obvious typing problems (high confidence)

Plan:
- Remove unused imports and dead helpers if truly unused.
- Add/adjust type hints where it clarifies shapes (especially dict vs object results).

Verification:
- Pylance Problems reduced.
- No runtime behavior changes.

## Phase D — Validate random + list endpoints remain safe (30–60 min)

Plan:
- Confirm `/random` does not unnecessarily load 20k entries if backend cache supports a more efficient random selection.
  - Only optimize if there is an existing cache method that provides O(1) or O(log n) random.
  - Otherwise keep as-is (risk: performance vs correctness).

Verification:
- `GET /api/launchbox/random` returns a game quickly.
- `GET /api/launchbox/games/list` still returns an array.

## Phase E — VS Code tasks cleanup (optional, 15–30 min)

Plan:
- Deduplicate duplicate “Gateway Smoke” tasks in `.vscode/tasks.json`.
- Keep labels stable.

Verification:
- Task list no longer shows duplicates.

## Phase F — Add minimal safety net tests (recommended, 60–120 min)

Goal: protect the “high confidence” refactors from regressions.

Suggested tests under `tests/backend/`:
- `/resolve` supports both GET `title=` and POST body aliases.
- `/games` returns paginated object shape.
- `/games/list` returns list.

Guardrail:
- Keep tests fast; no network; use dependency overrides.

## Final Verification Checklist (must be GREEN)

- `python -m compileall backend`
- `pytest -q tests/backend` (or a small targeted subset)
- `npm run smoke:stack:no-start`
- `npm run smoke:gateway:no-start`

## Deliverables / Handoff Notes to Append

After completion, append a short “Session 2026-01-22” note to the root README:
- What was fixed
- What was intentionally NOT changed (risk)
- Exact verification commands that were run
